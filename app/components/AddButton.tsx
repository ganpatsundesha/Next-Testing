"use client"
import React from 'react'

const AddButton = () => {
    return (
        <div>
            <button onClick={() => console.log("test pass")}>Add to Cart</button>
        </div>
    )
}

export default AddButton